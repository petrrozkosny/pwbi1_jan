01 suma srazek ruzyne = 
SUMX(
    FILTER(
        data,
    data[lokalita] = "RUZYNE"),
data[srazky])


______________________________________

02 podil srazek = 
SUM(data[srazky]) / SUMX(ALL(data),data[srazky])
